import USER_TYPE from "$types/USER_TYPE"

export const getTableId = (userType: USER_TYPE) => `utility-login-users-table-${userType}`

const UtilityLoginView = () => {
  return (
    <>
    </>
  )
}

export default UtilityLoginView