import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as waitingRoomRouter,
  schemas as waitingRoomViewsSchemas,
  ROUTE as WAITING_ROOM_VIEW_ROUTE,
  routerPrefix as waitingRoomViewsRouterPrefix,
}
