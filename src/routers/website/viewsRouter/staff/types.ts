export enum ROUTE {
  DEPARTMENTS = "/departments",
  DEPARTMENT_SETTINGS = "/departments/:id",
  USERS = "/users",
  AI_SETTINGS = "/ai-settings",
  INSIGHTS = "/insights",
  DOCUMENTS = "/documents",
}
