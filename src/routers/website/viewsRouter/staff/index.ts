import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as staffRouter,
  schemas as staffViewsSchemas,
  ROUTE as STAFF_VIEW_ROUTE,
  routerPrefix as staffViewsRouterPrefix
}
