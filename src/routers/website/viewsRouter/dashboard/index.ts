import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"


export {
  router as dashboardRouter,
  schemas as dashboardViewsSchemas,
  ROUTE as DASHBOARD_VIEW_ROUTE,
  routerPrefix as dashboardViewsRouterPrefix
}
