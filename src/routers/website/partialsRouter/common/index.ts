import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as commonRouter,
  routerPrefix as commonPartialsRouterPrefix,
  schemas as commonPartialsSchemas,
  ROUTE as COMMON_PARTIALS_ROUTE,
}
