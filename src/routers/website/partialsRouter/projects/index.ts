import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as projectsPartialsRouter,
  routerPrefix as projectsPartialsRouterPrefix,
  schemas as projectsPartialsSchemas,
  ROUTE as PROJECTS_PARTIALS_ROUTE,
}
