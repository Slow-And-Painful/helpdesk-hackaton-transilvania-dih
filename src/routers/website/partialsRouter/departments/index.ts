import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as departmentsPartialsRouter,
  routerPrefix as departmentsPartialsRouterPrefix,
  schemas as departmentsPartialsSchemas,
  ROUTE as DEPARTMENTS_PARTIALS_ROUTE,
}
