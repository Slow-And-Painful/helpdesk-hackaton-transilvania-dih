import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as ticketsPartialsRouter,
  routerPrefix as ticketsPartialsRouterPrefix,
  schemas as ticketsPartialsSchemas,
  ROUTE as TICKETS_PARTIALS_ROUTE,
}
