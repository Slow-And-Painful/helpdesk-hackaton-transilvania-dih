import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as usersPartialsRouter,
  routerPrefix as usersPartialsRouterPrefix,
  schemas as usersPartialsSchemas,
  ROUTE as USERS_PARTIALS_ROUTE,
}
