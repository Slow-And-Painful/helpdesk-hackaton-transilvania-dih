import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as ticketsActionsRouter,
  routerPrefix as ticketsActionsRouterPrefix,
  schemas as ticketsActionsSchemas,
  ROUTE as TICKETS_ACTIONS_ROUTE,
}
