import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as staffActionsRouter,
  routerPrefix as staffActionsRouterPrefix,
  schemas as staffActionsSchemas,
  ROUTE as STAFF_ACTIONS_ROUTE,
}
