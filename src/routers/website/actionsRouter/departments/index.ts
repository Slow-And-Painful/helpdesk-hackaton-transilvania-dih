import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as departmentsActionsRouter,
  routerPrefix as departmentsActionsRouterPrefix,
  schemas as departmentsActionsSchemas,
  ROUTE as DEPARTMENTS_ACTIONS_ROUTE,
}
