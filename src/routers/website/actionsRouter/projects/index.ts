import { router, routerPrefix } from "./router"
import { schemas } from "./schemas"
import { ROUTE } from "./types"

export {
  router as projectsActionsRouter,
  routerPrefix as projectsActionsRouterPrefix,
  schemas as projectsActionsSchemas,
  ROUTE as PROJECTS_ACTIONS_ROUTE,
}
